// Lobby filter chips: show the tiles whose data-tags include the chosen filter.
// (Owner: lobby. Markup contract: [data-filters] holds button.chip[data-filter]
// and [data-filter-count]; tiles are li[data-tags] inside [data-lobby].)
export function startLobby() {}
